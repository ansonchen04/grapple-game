using Godot;
using System;

public partial class test : Node2D
{	
	/*
	private PackedScene Rope = (PackedScene)GD.Load("res://rope/Rope.tscn");
	private Vector2 startPos = Vector2.Zero;
	private Vector2 endPos = Vector2.Zero;

	public override void _Input(InputEvent @event)
	{
		if (@event is InputEventMouseButton mouseEvent && mouseEvent.Pressed)
		{
			if (startPos == Vector2.Zero)
			{
				startPos = GetGlobalMousePosition();
			}
			else if (endPos == Vector2.Zero)
			{
				endPos = GetGlobalMousePosition();
				// Instance the Rope scene
				var rope = (Rope) Rope.Instantiate();
				AddChild(rope);
				// Assuming Rope has a method called SpawnRope
				// rope.Call("spawn_rope", startPos, endPos);
				startPos = Vector2.Zero;
				endPos = Vector2.Zero;
			}
		}
	}
	*/
}
